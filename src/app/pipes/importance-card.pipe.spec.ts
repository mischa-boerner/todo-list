import { ImportanceCardPipe } from './importance-card.pipe';

describe('ImportanceCardPipe', () => {
  it('create an instance', () => {
    const pipe = new ImportanceCardPipe();
    expect(pipe).toBeTruthy();
  });
});
